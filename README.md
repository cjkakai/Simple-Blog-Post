# Simple Blog Post Manager

A simple frontend project that displays and manages blog posts using JSON Server.

## Features

- View blog posts with title, image, content, and author
- Click to view full post details
- Submit new blog posts via form
- Uses a mock backend powered by `json-server`

## Getting Started

1. Clone this repository:
   ```bash
   git clone git@github.com:yourusername/Simple-Blog-Post.git
Navigate into the project folder:

bash
Copy code
cd Simple-Blog-Post
Install JSON Server (if you haven’t already):

bash
Copy code
npm install -g json-server@0.17.4
Start the JSON Server:

bash
Copy code
json-server db.json
Open index.html with Live Server.

Folder Structure
pgsql
Copy code
.
├── index.html
├── db.json
├── css/
│   └── styles.css
└── src/
    └── index.js
